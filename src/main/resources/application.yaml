spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pl_data
    username: postgres
    password: --------
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true